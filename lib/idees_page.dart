import 'package:flutter/material.dart';


class IdeesPage extends StatelessWidget {
  const IdeesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Boite à idées"),
      ),
    );
  }
}
